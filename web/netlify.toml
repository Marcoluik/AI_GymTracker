[build]
  base    = "web"
  command = "npm run build"
  publish = "dist"

# SPA: route everything to index.html so React Router handles client-side routes.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
